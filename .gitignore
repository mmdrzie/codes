Nothing needs to be added to .gitignore based on the provided file changes. The changes consist of a shell script (`install-oqs.sh`) and a TypeScript source file (`src/services/crypto/pq-crypto-service.ts`), which are both legitimate project files that should not be ignored.